李培煜Q1 定性：这就是一个计算几何题目
目标：求出包围S点的凸多边形，并且求直径
所需数据：各个检测点的坐标以及θ角度
基本假设：每次有效示向度与真实方位角的圆周角度偏差不超过1°；认为监测点不存在距离过近、无法测向；假设全部观测对应的楔形区域的交集有界

求解方法：逐次求交，不断缩小干扰源的可行区域。
![image-1](image-1.png)
得到凸多边形后，提取顶点的集合，计算
![alt text](image.png)
由于我们得出的一定是一个凸包（这里需要一定的证明），然后直接使用旋转卡壳算法，得到最大值（直径）。
直径圆能否直接盖住多边形？肯定是不行的我们可以举反例：
![alt text](image-2.png)
结果检验：
可以随机生成几个测试集，然后搞实测形象图。其实没什么好检验的，这是一个计算几何题
