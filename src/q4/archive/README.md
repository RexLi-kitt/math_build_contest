# Q4 源码归档

本目录保存旧版 RAHPS-B 兼容入口和历史字节码，不属于当前 V13 运行链路。

- `solve_q4_legacy.py` 依赖项目根目录的旧共享模块，仅用于追溯；
- `stale_bytecode/` 是历史编译产物，不可作为源码执行或复现依据；
- 正式模型、测试和结果分别见 `../versions/`、`../tests/` 和 `../../../outputs/q4/v13/`。
