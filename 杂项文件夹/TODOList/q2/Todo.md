1.给出关键代码段 （已完成）
    要求：
        选出核心算法程序（有几个选几个）
        选出每个算法程序的核心代码段，以一个新的程序保存
        在这个程序的开头，写一行注释：一行字概括说明这段程序的功能
        在src中，创建新的文件夹，用来存放这些代码程序，文件夹取名为核心代码段
        程序命名格式：q1_solver.py / q1_exact_verify.py

2.关键词：主动感知定位（Active Target Localization）
（已完成）

3.摘要：
针对问题二，考虑首次测向的±1°误差、接收半径未知与移动代价，构建物理可行域，建立兼顾几何质量、保证接收比例、移动时间和预测定位半径的四指标主动选点模型。采用粗搜—局部加密求解，并用蒙特卡洛—网格搜索校准权重。独立回放中接收率达100%，二测平均最小外接圆半径42.62 m，总时间均值109 s，较对照策略兼顾精度与时效性，可为后续清除判定提供第二检测点。

（已完成）
