1.给出关键代码段 （已完成）
    要求：
        选出核心算法程序（有几个选几个）
        选出每个算法程序的核心代码段，以一个新的程序保存
        在这个程序的开头，写一行注释：一行字概括说明这段程序的功能
        在src中，创建新的文件夹，用来存放这些代码程序，文件夹取名为核心代码段
        程序命名格式：q1_solver.py / q1_exact_verify.py

2.关键词：主动感知定位（Active Target Localization）
（已完成）

3.摘要：
针对问题二，考虑第二检测点选择中定位精度、接收可靠性与移动代价的冲突，构建物理可行域，建立兼顾几何质量、保证接收比例、移动时间和定位误差代理的四指标选点模型，采用全局粗搜—局部加密和蒙特卡洛权重校准求解。在40个合成回放场景中，二测接收率为100%，平均实际MEC为42.62 m，二测移动与检测平均耗时109 s，整体优于对照策略，并以MEC≤20 m作为后续清除判据。

（已完成）
