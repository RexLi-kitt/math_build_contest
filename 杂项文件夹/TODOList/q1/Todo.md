1.给出关键代码段 （已完成）
    要求：
        选出核心算法程序（有几个选几个）
        选出每个算法程序的核心代码段，以一个新的程序保存
        在这个程序的开头，写一行注释：一行字概括说明这段程序的功能
        在src中，创建新的文件夹，用来存放这些代码程序，文件夹取名为核心代码段
        程序命名格式：q1_solver.py / q1_exact_verify.py

    已完成情况（代码位于 src/q1/核心代码段/）：
        q1_solver.py：示向度 ±1° 误差带 → 闭半平面 → 半平面交，得到凸定位区域并判定空集/无界/点/线段/多边形；
        q1_exact_verify.py：旋转卡壳（与全顶点枚举互验）求区域直径，并判定以 D 为直径的同直径圆能否覆盖区域。
    自查：与 src/q1/localization.py 在 200 组随机观测上状态、顶点、直径平方、最远点对、覆盖圆参数完全一致；
          等边三角形反例 D=20 m、R_min=20/√3≈11.547 m>D/2，判定「不能覆盖」，与方案卡一致。

2.算法思想：每条示向度只在 ±1° 误差带内有效，故把每次观测写成张角 2° 的闭楔形（两个闭半平面），
  逐次求交得到凸的定位区域 P；区域非空有界时由「直径必在顶点对取到」用旋转卡壳求 D，
  再用「以 D 为直径的同直径圆」判定覆盖——半径 D/2 的圆若含最远点对则圆心只能是其中点，
  故只检查最远顶点是否超出 D/2 即可，等边三角形反例说明结论是「不一定能覆盖」。
关键词：示向度交会凸区域定位（Bearing-only Convex Localization）
（已完成）
